# Q21. Write a program to count the number of digits in an integer.

numbers = int(input("Enter some digit of numbers: "))

numbers = str(numbers)

print(f"There are total {len(numbers)} digits in {numbers}.")
