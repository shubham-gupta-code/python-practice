# Q2. Take user input and display it back to the user.

user_input = input("Enter something: ")   # input() takes values given by user in a string type
print(f"You type: {user_input}")

# 'f' --> format strings
# 'f' makes strings able to use variables of any data types.